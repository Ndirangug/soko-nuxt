query Categories( $pagination: Pagination) {
  categories(pagination: $pagination) {
    title
    description
    products {
      productID
      title
      shortDescription
      price
      thumbnails
    }
  }
}